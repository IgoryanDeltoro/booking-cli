const ctrlWrapper = require('../helpers/ctrlWrapper');
const { validateBody, validateFormDataBody } = require('./validateBody');

module.exports = {
  ctrlWrapper,
  validateBody,
  validateFormDataBody,
};
